services:
  food-tracker:
    build: .
    container_name: food-tracker-dev
    restart: unless-stopped
    ports:
      - "8686:8787"
    volumes:
      - food-data-dev:/root/.local/share/food
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  food-data-dev:
